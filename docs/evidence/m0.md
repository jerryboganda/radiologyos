# M0 evidence — Foundation

- Verdict: **pending**. Nothing below has been recorded yet. M0 is not accepted.
- SHA: TODO: the 40-character SHA of the next deploy (dispatched only with the owner's
  OK, ADR 0011) · deployed TODO UTC · alembic head TODO
- Recorded by: TODO
- Format: [`README.md`](README.md). Scope: ADR 0008, amended by ADR 0022.

The previous deploy (`0a18f2c`, head `20260926_0014`) is **not** used here. `main`
has since gained further migrations (work packages B–J), so the record must match
the head of whatever is deployed next.

## Workflow runs

TODO: paste the output of `python scripts/evidence_record.py <sha>` unedited.

| Workflow | Run | Status | Conclusion | Created (UTC) |
| --- | --- | --- | --- | --- |
| CI | TODO | TODO | TODO | TODO |
| Build images | TODO | TODO | TODO | TODO |
| Deploy production | TODO | TODO | TODO | TODO |
| Verify production RLS | TODO | TODO | TODO | TODO |
| Verify production identity | TODO | TODO | TODO | TODO |

Same-SHA evidence: TODO.

## Drill and checks

| Check | Source | Result |
| --- | --- | --- |
| Restore drill on the deployed head (checksums, restored and live head, RLS tables, extensions, Keycloak realm) | `infra/ops/backup-restore-drill.sh` | TODO: paste the summary unedited |
| RPO / RTO | drill output | TODO |
| Off-host copy is fresh (under 26 h) | `offsite-backup.last-success` | TODO |
| Resource discipline, zero violations | `check-compliance.sh` | TODO |

## Milestone checks

| Check | Source | Result |
| --- | --- | --- |
| Runtime role is NOSUPERUSER, NOBYPASSRLS, owns nothing, has no `CREATE` | `Verify production RLS` log | TODO |
| Every `tenant_id` table: ENABLE + FORCE RLS, empty without context, refuses a foreign insert; catalog equals the data-rights registry | `Verify production RLS` log | TODO |
| OIDC login, token checks, tenant-switch 403, forged-credential 401, role denial, logout refused refresh | `Verify production identity` log | TODO |
| Former `/v1/preview/*` paths answer 404 | `check-preview-gated.py` in `Verify production identity` | TODO |
| Static and dependency security (Ruff, mypy, Bandit, pip-audit, npm audit) | `CI` | TODO |
| Generated OpenAPI committed and valid | `CI` | TODO |
| Health and readiness inside the platform network, no published ports | `Deploy production` log | TODO |
| Browser E2E on the Compose stack | `E2E` run for the same SHA | TODO |
| Security disposition of the dev-only Playwright advisories (G33) | TODO | TODO |
| Open critical or high findings | Dependabot, CodeQL | TODO |

## Demo

[`demos/m0-foundation.md`](../demos/m0-foundation.md) · run TODO on TODO · skipped
steps: TODO.

## Open limitations

- No person signs off on security, privacy or legal questions; the automated gate
  stands in for that (ADR 0008).
- Admins can bypass branch protection for direct pushes, so the post-deploy
  verification is the real gate (ADR 0022).
- TODO: add any limitation found while recording.
