# M0 evidence — Foundation

- Verdict: **accepted** under ADR 0008 as amended by ADR 0022. Every automated gate is
  green for the same SHA. The limitations are listed at the end.
- SHA: `58f385968a9383dbca76e02e618eccc8f92a0f16` · deployed 2026-09-26T12:36Z UTC with the
  owner's explicit OK · alembic head `20260926_0103`
- Recorded by: Claude Code (orchestrator session), 2026-09-26
- Format: [`README.md`](README.md). Scope: ADR 0008, amended by ADR 0022.

## Workflow runs

Output of `python scripts/evidence_record.py 58f385968a9383dbca76e02e618eccc8f92a0f16`:

| Workflow | Run | Status | Conclusion | Created (UTC) |
| --- | --- | --- | --- | --- |
| CI | [36241731309](https://github.com/jerryboganda/radiologyos/actions/runs/36241731309) | completed | success | 2026-09-26T12:22:50Z |
| Build images | [36241731311](https://github.com/jerryboganda/radiologyos/actions/runs/36241731311) | completed | success | 2026-09-26T12:22:50Z |
| Deploy production | [36242449879](https://github.com/jerryboganda/radiologyos/actions/runs/36242449879) | completed | success | 2026-09-26T12:36:20Z |
| Verify production RLS | [36242520030](https://github.com/jerryboganda/radiologyos/actions/runs/36242520030) | completed | success | 2026-09-26T12:37:42Z |
| Verify production identity | [36242519892](https://github.com/jerryboganda/radiologyos/actions/runs/36242519892) | completed | success | 2026-09-26T12:37:42Z |

Same-SHA evidence: **all green**.

## Drill and checks

| Check | Source | Result |
| --- | --- | --- |
| Restore drill on the deployed head | `infra/ops/backup-restore-drill.sh`, run on the host after a fresh `backup.sh` (dump set 20260926) | **RESTORE VERIFIED**: checksums pass; restored and live head are both `20260926_0103`; 51 tenant tables restored (51 live), all ENABLE + FORCE RLS; `vector`, `pg_trgm` and the `app` schema present; the Keycloak dump restores the `radbrain` realm |
| RPO / RTO | drill output | RPO 0h (fresh dump) · RTO 13s |
| Off-host copy | `/var/log/radiologyos-offsite-backup.log` | DB and Keycloak dumps for 20260926 uploaded and verified at 08:26Z. The first object-store seed was still uploading at recording time, so there is no `.last-success` marker yet. A nightly run at 03:30 local is scheduled under `flock` |
| Resource discipline | `/opt/platform/bin/check-compliance.sh` | **0 violations attributable to radiologyos**. The 8 findings belong to other projects on the shared host |

## Milestone checks

| Check | Source | Result |
| --- | --- | --- |
| Runtime role is NOSUPERUSER and NOBYPASSRLS, owns nothing, and has no `CREATE` | `Verify production RLS` run 36242520030 | pass |
| Every `tenant_id` table: ENABLE + FORCE RLS, empty without context, refuses a foreign insert; the catalog equals the data-rights registry | `Verify production RLS` run 36242520030 | pass |
| OIDC login, token checks, tenant-switch 403, forged-credential 401, role denial, and a refresh refused after logout | `Verify production identity` run 36242519892 | pass |
| Former `/v1/preview/*` paths answer 404 | `check-preview-gated.py` in `Verify production identity` | pass |
| Static and dependency security (Ruff, mypy, Bandit including `packages/`, pip-audit, npm audit) | `CI` run 36241731309 | pass |
| Generated OpenAPI committed and valid | `CI` run 36241731309 | pass |
| Health and readiness inside the platform network, with no published ports | `Deploy production` run 36242449879 | pass |
| Browser E2E on the Compose stack | `E2E` run [36241731342](https://github.com/jerryboganda/radiologyos/actions/runs/36241731342) | pass |
| Playwright advisories (dev-only, G33) | Dependabot | resolved: `@playwright/test` bumped to 1.55.1 |
| Open critical or high findings | Dependabot, CodeQL | **none open**. CodeQL alert 1 was dismissed as a false positive: a test asserting prompt text. Dependabot alert 4 (`transformers`) was dismissed as a tolerable risk, because it is only in the switched-off, undeployed embedder image. Two low alerts remain (`torch`, `cookie`) |

## Demo

[`demos/m0-foundation.md`](../demos/m0-foundation.md). Not yet run live with the owner.
The script covers only steps that the runs above already evidence.

## Open limitations

- No person signs off on security, privacy or legal questions; the automated gate
  stands in for that (ADR 0008).
- Admins can bypass branch protection for direct pushes, so the post-deploy
  verification is the real gate (ADR 0022).
- Admin MFA (ADR 0032) is configured in code but not yet applied to the live realm. It is
  applied once `verify-oidc.py` signs in with a non-admin verification account.
- The first off-host object-store seed had not finished when this was recorded.
