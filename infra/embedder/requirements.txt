# radbrain embedder runtime (ADR 0019). Exact pins; the image build smoke-tests them.
# CPU-only torch: the default PyPI Linux wheel pulls several GB of CUDA libraries.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.1+cpu
# Capped at 5.8.1: the pinned model revision's remote code calls
# create_causal_mask(input_embeds=..., cache_position=...), which 5.9.0 removed.
transformers==5.10.1
sentence-transformers==5.5.1
huggingface-hub==1.15.0
tokenizers==0.22.2
fastapi==0.141.1
uvicorn==0.54.0
