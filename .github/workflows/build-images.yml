name: Build images

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAMESPACE: ${{ github.repository_owner }}

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: python
            context: .
            dockerfile: infra/python/Dockerfile
            image: ghcr.io/${{ github.repository_owner }}/radiologyos-python
          - name: web
            context: ./apps/web
            dockerfile: apps/web/Dockerfile
            image: ghcr.io/${{ github.repository_owner }}/radiologyos-web
          # Local voyage-4-nano (ADR 0019); the build smoke-tests the baked model.
          - name: embedder
            context: ./infra/embedder
            dockerfile: infra/embedder/Dockerfile
            image: ghcr.io/${{ github.repository_owner }}/radiologyos-embedder

    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ matrix.image }}
          tags: |
            type=sha,format=long
            type=raw,value=${{ github.sha }}
            type=raw,value=latest

      - uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ matrix.name }}
          cache-to: type=gha,mode=max,scope=${{ matrix.name }}
